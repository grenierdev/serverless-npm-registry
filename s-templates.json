{
	"npmEnvironments": {
		"NPM_SECRET": "${secret}",
		"NPM_USER_TABLE": "${project}-users-${stage}",
		"NPM_PACKAGE_TABLE": "${project}-packages-${stage}",
		"NPM_PACKAGE_BUCKET": "${project}-packages-${stage}"
	}
}
